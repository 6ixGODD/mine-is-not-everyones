# Ephemeral Reports

Implementation, review, synchronization, and release-closure reports live here while the temporary plan workspace exists.

Reports are evidence, not durable product documentation. Accepted facts that matter after release are incorporated into `docs/design/` before workspace closure.

This directory is deleted with `docs/plan/` before stable release integration.
